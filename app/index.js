import { registerRootComponent } from 'expo';  // This ensures the app is compatible with Expo
import App from './app';  // Adjust this path if necessary

// This function ensures that the app is registered and ready to be launched.
registerRootComponent(App);
